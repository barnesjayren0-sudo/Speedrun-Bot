org.gradle.jvmargs=-Xmx2G

minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.14.25
fabric_version=0.92.2+1.20.1

# Optional: set when you drop official Baritone Fabric jar into libs/
# baritone is loaded by reflection so the mod still builds without it

mod_version=1.1.0
mod_name=Speedrun-Bot
maven_group=com.speedrunbot
archives_base_name=speedrun-bot
